spring.application.name=CivicConnectBackend
spring.security.user.name=ankit
spring.security.user.password=hunter2580

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/CivicConnect
spring.datasource.username=postgres
spring.datasource.password=hunter2580

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# PostgreSQL Dialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Server Port
server.port=8080

jwt.secret=${JWT_SECRET:MySecretKeyMySecretKeyMySecretKeyAnkitSecretKeyHunterXHunter}

jwt.expiration=360000000